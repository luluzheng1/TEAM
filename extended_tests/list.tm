list<string> classes = ["HCI", "Programming Languages", "Compilers", "Security"];
print(classes[0]);
list<string> funClasses = classes[1:3];
for (int i = 0; i < len(funClasses); i++):
  print(funClasses[i]);
end
